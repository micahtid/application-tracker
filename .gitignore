PLANNING.md
PRD.md
LOOP.md
LOOP2.md
LOOP3.md

node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# Secrets and local data. .env.local holds the key that decrypts the API key.
.env.local
.env*.local
prisma/*.db
prisma/*.db-journal

.DS_Store
Thumbs.db
prisma/check.db*

# Everything the loop harness produces. It holds real mail (LOOP 3.1).
# Anchored, so it does not also swallow scripts/loop, which is source.
/loop/

