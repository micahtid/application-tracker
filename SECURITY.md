# Security

The app holds a Gmail refresh token and an encrypted API key, so report anything that could reach
either of them privately rather than in an issue.

Use **Security > Report a vulnerability** on this repository. That opens an advisory only the
maintainer can see.

Say what you did and what you saw. Do not include real email content.
