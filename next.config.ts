import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The app runs on one machine for one person. Nothing here is served publicly.
  reactStrictMode: true,
};

export default nextConfig;
